<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:IMCCodingTest.ViewModels"
             x:Class="IMCCodingTest.Views.FrontPage">
    <ContentPage.BindingContext>
        <vm:FrontPageViewModel/>
    </ContentPage.BindingContext>
    <StackLayout VerticalOptions="Center">
        <Label HorizontalOptions="Center">Enter Order Details</Label>
        <Button HorizontalOptions="Center" Text="Create" Command="{Binding CreateOrderCommand}"/>
    </StackLayout>

</ContentPage>
